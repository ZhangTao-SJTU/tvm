Layout of relevant contents in toolbox:

1. minimization.FIREminimization:

    Attributes:

    - _config stores a tissueSample.Sample periodic tissue.
    (at initializationloaded from a configuration folder, which also includes conf and cellParameters.input)
    - _dir
    - _modified_cells
    Methods:
    - @classmethod from_config(config_dir, input_filename)
    Given an inpu
    - minimize_config() 
    FIRE minimizes this configuration
    - write_configuration(filename)
    - load_cell_parameters()

